# GuardianAI Core 🛡️

**Движок нового поколения для обнаружения фишинга и мошенничества**

GuardianAI — это продвинутый AI-бэкенд, разработанный для защиты пользователей от социальной инженерии, фишинга и мошеннических атак. Он обеспечивает работу **Android-приложения GuardianAI** и десктопных клиентов, предоставляя анализ текста в реальном времени с объяснимыми вердиктами.

![Status](https://img.shields.io/badge/Status-Production-green) ![Python](https://img.shields.io/badge/Python-3.10%2B-blue) ![API](https://img.shields.io/badge/API-FastAPI-009688) ![Model](https://img.shields.io/badge/Model-RuBERT%20%2B%20ONNX-orange)

---

## 🧠 Ключевые возможности

### 1. Гибридная архитектура интеллекта

В отличие от простых фильтров по ключевым словам, GuardianAI объединяет три уровня защиты:

*   **Глубокое обучение (Deep Learning - RuBERT Tiny2)**: Модель на основе трансформеров, оптимизированная для понимания нюансов русского языка и намерений (экспортирована в ONNX для скорости).
*   **Машинное обучение (Random Forest)**: Анализирует статистические признаки текста (количество заглавных букв, пунктуация, длина).
*   **Эвристический движок (Regex)**: Обнаружение известных паттернов мошенничества, криптокошельков и вредоносных ссылок с помощью регулярных выражений.

### 2. Объяснимый ИИ (XAI) 💡

Система не просто говорит "SCAM" (Мошенничество) — она объясняет **ПОЧЕМУ**:

*   **Анализ влияния**: Определяет конкретные слова, которые спровоцировали ИИ (тест динамического перекрытия/occlusion test).
*   **Распознавание сущностей (NER)**: Извлекает организации ("Сбербанк"), персон ("Мама") и денежные суммы, проверяя контекст их использования.
*   **Подсветка триггеров**: Визуализирует опасные паттерны прямо в тексте.

### 3. Контекстная осведомленность 📱

Движок понимает источник сообщения. Запрос "Обновите Telegram" безопасен, если контекст `["Telegram App"]`, но опасен, если контекст `["WhatsApp"]` или `["SMS"]`.

### 4. Умный охотник за ссылками (Link Hunter) 🕵️‍♂️

*   Обнаруживает гомоглифы (поддельные домены, похожие на настоящие, например, с заменой букв).
*   Проверяет безопасность протокола (HTTP vs HTTPS).
*   Проверяет наличие IP-адресов вместо доменов.
*   Валидирует по белому списку официальных доменов.

---

## 🏗️ Структура и Логика нейросети

GuardianAI построен по модульному принципу, где каждый компонент отвечает за свою часть анализа:

1.  **`src/core/classifier.py`**: Главный мозг системы. Управляет загрузкой моделей (ONNX/PyTorch), объединяет предсказания от BERT и Random Forest, применяет пороги срабатывания.
2.  **`src/core/explainer.py`**: Отвечает за интерпретируемость. Использует методы LIME-like (удаление слов) для понимания того, что именно повлияло на решение модели.
3.  **`src/core/ner.py`**: Модуль NER (Named Entity Recognition) на базе Natasha. Ищет имена, организации и, что важно, связывает их с ключевыми словами "срочности" или "денег".
4.  **`src/core/link_hunter.py`**: Специализированный анализатор URL.

**Логика принятия решения:**
1.  Текст нормализуется (убираются гомоглифы).
2.  Проверяется **Белый список**. Если текст доверенный -> `SAFE`.
3.  Ищутся **Regex-паттерны** (быстрая проверка).
4.  **ML-модель** (BERT + Random Forest) оценивает вероятность мошенничества.
5.  **Link Hunter** проверяет ссылки.
6.  Финальный вердикт выносится по максимальной угрозе (`max(ML, Links, Patterns)`), с учетом эвристик (например, упоминание ФСБ + требование перевода денег).

---

## 🔌 API Reference

Ядро работает как сервис **FastAPI**.

### `POST /predict`

Анализирует сообщение на вероятность мошенничества.

**Запрос:**

```json
{
  "text": "Выиграй приз на сайте http://fake-casino.com!",
  "strict_mode": false,
  "context": ["SMS"]
}
```

**Ответ:**

```json
{
  "is_scam": true,
  "score": 0.99,
  "verdict": "DANGEROUS",
  "reason": ["⛔ Подозрительный контекст", "⚠️ Фишинговая ссылка"],
  "explanation": [
    {"word": "casino", "type": "TRIGGER", "impact": 1.0}
  ]
}
```

---

## 🚀 Установка и Запуск

### Предварительные требования

*   Python 3.10+
*   NVIDIA GPU (Опционально, рекомендуется для обучения)

### 1. Клонирование и установка

```bash
git clone https://github.com/f4rceful/GuardianAI.git
cd GuardianAI

# Установка зависимостей
pip install -r requirements.txt
```

### 2. Запуск API Сервера

Запустите бэкенд-сервис на порту 8000:

```bash
python src/api/server.py
```

*Сервер автоматически загрузит ONNX модель и инициализирует пайплайн.*

### 3. Запуск Desktop UI (Опционально)

Для тестирования можно запустить дашборд на Flet:

```bash
python src/ui/main.py
```

---

## 📂 Структура Проекта

```text
GuardianAI/
├── dataset/                 # Наборы данных для обучения
│   ├── safe_samples.txt     # Примеры безопасных сообщений
│   └── scam_samples.txt     # Примеры мошенничества
├── models/                  # Обученные модели (ONNX, Joblib)
├── src/                     # Исходный код
│   ├── api/
│   │   └── server.py        # Бэкенд на FastAPI
│   ├── core/                # Ядро логики нейросети
│   │   ├── classifier.py    # Главный класс (Оркестратор)
│   │   ├── explainer.py     # Модуль объяснимости (почему это скам?)
│   │   ├── features.py      # Извлечение стат. признаков
│   │   ├── link_hunter.py   # Анализ ссылок и доменов
│   │   ├── ner.py           # Поиск сущностей (Natasha)
│   │   ├── patterns.py      # База Regex паттернов
│   │   └── sentiment.py     # Анализ тональности (BERT)
│   ├── ui/                  # Графический интерфейс
│   │   ├── main.py          # Точка входа приложения
│   │   └── views.py         # Отрисовка экранов
│   └── utils/
│       └── text_processing.py # Очистка и нормализация текста
├── tests/                   # Набор тестов
│   ├── benchmark_model.py   # Тест скорости
│   └── verify_accuracy.py   # Проверка точности
├── Dockerfile               # Конфигурация сборки контейнера
├── requirements.txt         # Зависимости Python
└── train.py                 # Скрипт запуска обучения
```

---

*Разработано для "Больших Вызовов" (Кванториум).*
